default_stages: [push]

repos:
  - repo: local
    hooks:
      - id: run-tests
        name: Run tests with full Conda context
        entry: bash -l ./scripts/run_tests.sh
        language: system
        pass_filenames: false
        stages: [push]
